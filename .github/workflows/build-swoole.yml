name: Build Swoole Extension (Docker)

on:
  workflow_dispatch:
    inputs:
      swoole_version:
        description: 'Swoole 版本 (留空则安装最新稳定版)'
        required: false
        default: ''
      php_version:
        description: 'PHP 版本 (例如 8.2)'
        required: true
        default: '8.5'  # 这里可以填 8.5

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        php-version: ['8.5']  # 可以根据需要添加

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build swoole.so inside Docker
        run: |
          PHP_VERSION="${{ matrix.php-version }}"
          SWOOLE_VERSION="${{ github.event.inputs.swoole_version }}"
          
          # 关键修改：不再指定发行版后缀，使用默认镜像（基于 Debian 12, glibc 2.36）
          IMAGE="php:${PHP_VERSION}-cli"
          
          cat > build-swoole.sh << 'EOF'
          #!/bin/bash
          set -e
          
          apt-get update
          apt-get install -y --no-install-recommends \
              build-essential \
              autoconf \
              libssl-dev \
              git \
              unzip \
              pkg-config
          
          if [ -n "$SWOOLE_VERSION" ]; then
              echo "Installing Swoole $SWOOLE_VERSION from source..."
              git clone --branch v$SWOOLE_VERSION --single-branch https://github.com/swoole/swoole-src.git /tmp/swoole
              cd /tmp/swoole
              phpize
              ./configure --enable-openssl --enable-http2
              make -j$(nproc)
              make install
          else
              echo "Installing latest Swoole via pecl..."
              pecl install swoole
          fi
          
          EXT_DIR=$(php -r "echo ini_get('extension_dir');")
          mkdir -p /output
          cp "$EXT_DIR/swoole.so" /output/
          echo "swoole.so copied to /output/"
          EOF
          
          chmod +x build-swoole.sh
          
          docker run --rm \
              -v $(pwd):/workspace \
              -w /workspace \
              -e SWOOLE_VERSION="$SWOOLE_VERSION" \
              "$IMAGE" \
              bash -c "bash build-swoole.sh && cp /output/swoole.so /workspace/"
          
          if [ ! -f "swoole.so" ]; then
              echo "❌ Failed to generate swoole.so"
              exit 1
          fi
          echo "✅ swoole.so generated successfully"

      - name: (可选) 检查生成的 so 依赖的 glibc 版本
        run: |
          echo "Highest GLIBC version required:"
          objdump -T swoole.so | grep GLIBC_ | sed 's/.*GLIBC_\([0-9.]*\).*/\1/g' | sort -uV | tail -1

      - name: Upload swoole.so
        uses: actions/upload-artifact@v4
        with:
          name: swoole-so-php-${{ matrix.php-version }}-default
          path: swoole.so
          retention-days: 30
